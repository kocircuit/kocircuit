import "github.com/kocircuit/kocircuit/lib/file/path" as filepath

Join(paths?) {
	return: filepath.GoJoinPath(paths)
}

Base(path?) {
	return: filepath.GoBasePath(path)
}

Dir(path?) {
	return: filepath.GoDirPath(path)
}
