import "github.com/kocircuit/kocircuit/lib/sync" as gosync // access go gates

// DialRing returns a synchronization ring, uniquely identified by its name and capacity.
DialRing(name?, cap) {
	return: Bug("not impl")
}
