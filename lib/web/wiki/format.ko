import "github.com/kocircuit/kocircuit/lib/strings"

FormPar(f?, a) {
	return: Par(Form(f: f, a: a))
}

Form(f?, a) {
	return: Format(
		format: f
		args: a
		withString: formatString(string?) {
			return: Yield(
				if: Not(strings.Equal(string, ""))
				then: Text(string)
			)
		}
		withArg: Return
	)
}

FormString(f?, a) {
	return: strings.Join(
		Format(format: f, args: a, withString: Return, withArg: Return)
	)
}
